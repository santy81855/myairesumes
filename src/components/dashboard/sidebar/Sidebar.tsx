import styles from "./Sidebar.module.css";

const Sidebar = () => {
    return (
        <section className={styles.container}>
            <p>sidebar</p>
        </section>
    );
};

export default Sidebar;
